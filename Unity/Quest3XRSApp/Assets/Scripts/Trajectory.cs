using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Trajectory : MonoBehaviour
{
    
    /*
    private List<Vector3> trajectory = new List<Vector3>();
    private List<double[]> joints = new List<double[]>();
    private TrajectoryHelperFunctions HelperFunctions;

    public void ExecuteTrajectory() {
        StartCoroutine(ExecuteTrajectoryCoroutine());
    }

    IEnumerator ExecuteTrajectoryCoroutine() {
        
        foreach (var jointAngles in joints) {
            HelperFunctions.Set
            yield return new WaitForSeconds(0.1f);
        }
    }
    */
}
